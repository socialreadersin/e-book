{
  "name": "socialreaders-ebook",
  "version": "1.0.0",
  "private": true,
  "description": "Social Readers - Fullstack Frontend with Dual-Transport Firebase, Cloudflare & Cloudinary",
  "scripts": {
    "build": "node build-config.js",
    "start": "python3 -m http.server 8080",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
