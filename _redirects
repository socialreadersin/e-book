# Cloudflare Pages — URL Redirects & SPA Routing
#
# Format: [from] [to] [status_code]
# ─────────────────────────────────────────────────────────────────

# Admin shortcuts
/admin         /admin/dashboard.html    301
/dashboard     /admin/dashboard.html    301

# URL aliases for cleaner navigation
/catalog       /books.html              301
/listen        /audiobooks.html         301
/shop          /books.html              301
/library       /account.html            301
/account       /account.html            200
/login         /auth.html               301

# ─── SPA CATCH-ALL ───────────────────────────────────────────────
# This MUST be the last rule. Serves index.html for any path that
# doesn't match a static file, enabling client-side routing without 404s.
# CRITICAL for Cloudflare Pages deployment.
/*             /index.html              200
